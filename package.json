{
  "name": "chrome-extensions-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "extensions/*",
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}